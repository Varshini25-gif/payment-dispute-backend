# Payment Dispute Backend

This repository contains a FastAPI backend for payment dispute workflows.

## Features

- FastAPI application entry point in `app/main.py`
- Environment configuration via `python-dotenv`
- Central logging setup in `app/core/logging.py`
- Health endpoint at `/api/health`
- Basic unit test for the health endpoint
